# <프론트엔드_프로젝트_루트>/.env.development

# 로컬 백엔드 API 서버 주소
# (사용자님의 로컬 백엔드 서버가 http://192.168.0.13:3003 또는 http://localhost:3003 에서 실행된다고 가정)
VITE_API_HANDLER_URL=http://localhost:3003

# 로컬 개발 시 사용할 GitHub OAuth Client ID (운영과 동일한 값 사용)
VITE_GITHUB_OAUTH_CLIENT_ID="Ov23liSLlbepVJ4rW9RM"

# 로컬 개발 시 사용할 GitHub OAuth Redirect URI (프론트엔드 기준)
# 이 주소는 GitHub OAuth App 설정의 "Authorization callback URLs" 목록에 반드시 추가되어야 합니다.
# 또한, 백엔드 server.js의 .env 파일에 있는 GITHUB_OAUTH_REDIRECT_URI와도 일치해야 합니다.
# (사용자님께서 백엔드 .env에 http://localhost:50011/callback 으로 설정하셨던 것을 기준으로 합니다.)
VITE_GITHUB_OAUTH_REDIRECT_URI="http://localhost:50011/callback"