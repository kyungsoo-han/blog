# <프론트엔드_프로젝트_루트>/.env.production

# 운영(배포된) 백엔드 API 서버 주소
VITE_API_HANDLER_URL=https://note-api.hanks.kr

# 운영 환경용 GitHub OAuth Client ID (개발과 동일한 값 사용)
VITE_GITHUB_OAUTH_CLIENT_ID="Ov23li2TxE2go0b45XMs"

# 운영 환경용 GitHub OAuth Redirect URI
# 이 주소는 GitHub OAuth App 설정의 "Authorization callback URLs" 목록에 반드시 추가되어야 합니다.
# 또한, 운영 백엔드 server.js의 .env 파일에 있는 GITHUB_OAUTH_REDIRECT_URI와도 일치해야 합니다.
VITE_GITHUB_OAUTH_REDIRECT_URI="https://note.hanks.kr/callback"